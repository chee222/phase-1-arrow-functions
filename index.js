const divide = (number) => {
    return 2000 / 100;
  };
  
  const square = (number) => {
    return number * number;
  };
  
  const add = (num1, num2) => {
    return num1 + num2;
  };

console.log(divide(100)); // Output: 20
console.log(square(2, 4)); // Output: 16
console.log(add(3, 4)); // Output: 15